# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android / IDE
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
.kotlin/

# Signing — never commit keys
*.jks
*.keystore
keystore.properties

# On-device model weights — downloaded at runtime, multi-GB, never commit
*.litertlm
*.task
models/
app/models/

# Snapshot photos live in app-private storage on device; keep any local copies out
snapshots/
*.heic

# OS noise
.DS_Store
Thumbs.db
